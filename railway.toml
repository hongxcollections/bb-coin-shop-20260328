[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm build"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"

# Trigger redeploy with VITE_GOOGLE_CLIENT_ID
